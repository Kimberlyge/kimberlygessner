{{nav-main}}
<div class="Nav {{if layout.onIndex 'is-hidden'}}">
	{{#each model as |item|}}
		{{#link-to 'detail' item class="Nav-item"}}
			<p>{{item.title}}</p>
		{{/link-to}}
	{{/each}}
</div>
<main class="App-main">
	{{!-- don't move the outlet without changing liquid.scss --}}
	{{liquid-outlet}}
</main>
