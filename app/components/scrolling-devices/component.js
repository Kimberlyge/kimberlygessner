// /* global TimelineMax, TweenMax, Linear, Bounce */
// import gsap from 'npm:gsap';
// import {TimelineMax} from 'npm:gsap';
// import Ember from 'ember';

// const {Component, $, on} = Ember;

// export default Component.extend({

// 	sticky: on('didRender', function() {
// 		console.log('yo');
// 		var deviceMobile = $('.Device--mobile');
// 		var detailBody = $('.DetailBody');
// 		var deviceTablet = $('.Device--tablet');

// 		console.log(detailBody);

// 		var tl = new TimelineMax();

// 		tl.fromTo(detailBody, 0.5 {autoAlpha: 0}, {autoAlpha: 1});
// 		tl.to(deviceMobile, 1.5 {autoAlpha: 0});
// 		tl.fromTo(deviceTablet, 3.5 {autoAlpha: 0}, {autoAlpha: 1});
// 	});

// });
